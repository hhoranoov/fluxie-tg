{
	"name": "fluxie-tg",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"database": "wrangler d1 execute fluxie-tg --file db.sql --remote"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.6.4",
		"vitest": "~2.1.9",
		"wrangler": "^3.109.1"
	}
}
